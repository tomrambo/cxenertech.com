{
  "name": "cx-enertech",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build --preset cloudflare-pages",
    "build:node": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:seed": "node --import jiti/register ./scripts/seed-ev-packages.ts",
    "db:seed:solar": "node --import jiti/register ./scripts/seed-solar-packages.ts",
    "db:seed:all": "npm run db:seed && npm run db:seed:solar"
  },
  "dependencies": {
    "@nuxt/fonts": "^0.14.0",
    "nuxt": "^4.5.1",
    "vue": "^3.5.40",
    "vue-router": "^5.2.0"
  }
}
